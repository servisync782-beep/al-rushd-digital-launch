// Analytics & verification placeholders.
//
// These are intentionally empty so the site ships clean. Fill them in later
// (or wire them to a CMS / environment) to activate tracking and verification
// without touching component code.
export const ANALYTICS = {
  // Google Analytics 4 Measurement ID, e.g. "G-XXXXXXXXXX".
  gaMeasurementId: "",
  // Google Search Console verification token (content of the meta tag).
  gscVerification: "",
};
